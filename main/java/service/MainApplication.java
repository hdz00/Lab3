package service;

public class MainApplication {
    public static void main(String[] args) {
        // Call the main method of MainapplicationGUI to start the GUI
        MainApplicationGUI.main(args);
        // faturaDAO.insertFatura(qrCodeData[0], qrCodeData[1], qrCodeData[2]);

    }
}